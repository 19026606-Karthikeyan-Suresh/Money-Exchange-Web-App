@using FYP.Models;
@model List<CurrencyExchange>

    <div class="container py-sm-5">
            <header class="text-center text-black">
                <h1 class="display-4"><b>Exchange Rate</b></h1>
            </header>
            <div class="row py-5">
                <div class="col-sm-4 mx-auto">
                    <div class="card rounded shadow border-0">
                        <div class="card-body p-sm-5 bg-white rounded">
                            <div class="table-responsive">
                                <table class="table table-hover table-responsive" style="text-align:center">
                                    <thead>
                                        <tr>
                                            <th>Target Currency</th>
                                            <th>Rate (SGD$1)</th>
                                        </tr>
                                    </thead>

                                    <tbody>
                                        @foreach (CurrencyExchange item in Model)
                                        {
                                            <tr>
                                                <td>@item.Target_currency</td>
                                                <td>@item.Exchange_rate</td>
                                            </tr>
                                        }
                                    </tbody>
                                </table>
                            </div>
                        </div>
                    </div>
                </div>
            </div>
        </div>
