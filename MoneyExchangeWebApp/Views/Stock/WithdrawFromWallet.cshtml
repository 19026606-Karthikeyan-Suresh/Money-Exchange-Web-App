@model Stock

@*<div class="modal fade" id="DepositCurrencyModal" tabindex="-1" aria-labelledby="DepositCurrencyLabel" aria-hidden="true">
  <div class="modal-dialog modal-lg">
    <div class="modal-content">
      <div class="modal-header">
        <h2 class="modal-title" id="DepositCurrencyLabel">How much are you depositing?</h2>
        <button type="button" class="btn-close" data-bs-dismiss="modal" aria-label="Close"></button>
      </div>
      <div class="modal-body">*@
          <form method="post" asp-controller="Stock" asp-action="WithdrawFromWallet">

          <input asp-for="StockId" hidden readonly/>

          <div class="form-floating mb-3">
              <input type="text" class="form-control" asp-for="ISO" readonly>
               <label asp-for="ISO">ISO</label>
          </div>          
          <div class="form-floating mb-3">
              <input type="text" class="form-control" asp-for="Amount" readonly>
               <label asp-for="Amount">Current Balance</label>
          </div>
          <div class="form-floating mb-3">
              <input type="text" class="form-control" name="withdraw" id="withdraw">
               <label for="withdraw">Withdraw</label>
          </div>

@*          <div class="modal-footer">*@
              <div class="btn-group col-sm-12">
                  <a type="button" asp-controller="Stock" asp-action="ShowWallet" class="btn btn-outline-secondary mx-2">Go back to Index</a>
                  <button type="submit" class="btn btn-primary mx-2">Withdraw</button>
              </div>
@*          </div>*@
@if (ViewData["Message"] != null)
  {
      <div class="form-group row">
         <div class="offset-sm-2 col-sm-6">
            <div class="alert alert-@ViewData["MsgType"]">
               <text>@ViewData["Message"]</text>
            </div>
         </div>
      </div>
   }
          </form>
@*      </div>
    </div>
  </div>
</div>*@